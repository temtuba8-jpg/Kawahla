name: Keep Render Alive

on:
  schedule:
    - cron: '*/4 * * * *'

jobs:
  ping-server:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to website
        run: |
          curl -I https://https://kawahla.onrender.com/
